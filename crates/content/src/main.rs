//! MnemoKanji content pipeline — offline dataset builder (not shipped in the app).
//!
//! Builds `assets/seed.sqlite` from openly-licensed sources. See `docs/03-DESIGN.md` §8 and
//! `docs/08-DATASET.md`. M0 scaffold: placeholder entry point; real pipeline lands in M1.

fn main() {
    println!(
        "mnemokanji-content: dataset pipeline not yet implemented (M1). See docs/08-DATASET.md."
    );
}
